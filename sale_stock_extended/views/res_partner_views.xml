<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="view_partner_form_extended_2" model="ir.ui.view">
        <field name="name">res.partner.form.extended</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="is_IGM_customer"/>
                <field name="is_delivery_notify" />
                <field name="partner_responsible_rep" options="{'no_create':True}"
                       domain="['|', ('company_id', '=', False), ('company_id', '=', company_id)]"/>
            </xpath>
        </field>
    </record>
</odoo>

