<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="view_account_move_reversal_pl" model="ir.ui.view">
        <field name="name">view.account.move.reversal.inherit.pl</field>
        <field name="model">account.move.reversal</field>
        <field name="inherit_id" ref="account.view_account_move_reversal"/>
        <field name="mode">primary</field>
        <field name="priority">1000</field>
        <field name="arch" type="xml">
            <!--            <field name="refund_method" position="attributes">-->
            <!--                <attribute name="attrs">{'readonly': True}</attribute>-->
            <!--            </field>-->

            <field name="reason" position="replace">

                <!-- Vendor Bills / Vendor Credit Notes -->
                <field name="reason"
                       string="Corrected invoice number"
                       required="move_type.endswith('_refund') or move_type.endswith('_invoice')"
                       invisible="move_type not in ('in_invoice', 'in_refund')"/>

                <!-- Customer Invoices / Customer Credit Notes -->
                <field name="reason"
                       string="Reason"
                       required="move_type.endswith('_refund') or move_type.endswith('_invoice')"
                       invisible="move_type not in ('out_invoice', 'out_refund')"/>

            </field>

        </field>
    </record>


    <record id="action_view_account_move_reversal_pl" model="ir.actions.act_window">
        <field name="name">Reverse</field>
        <field name="res_model">account.move.reversal</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="trilab_invoice.view_account_move_reversal_pl"/>
        <field name="target">new</field>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list</field>
    </record>

</odoo>
